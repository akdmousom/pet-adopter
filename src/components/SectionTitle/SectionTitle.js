

const SectionTitle = ({ heading }) => {
    return (
        <div>
            <h1 className="text-3xl lg:text-5xl text-center font-bold pb-6 dark:text-white">{heading}</h1>
        </div>
    );
};

export default SectionTitle;