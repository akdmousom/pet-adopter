import Register from '@/Components/Register/Register';
import React from 'react';

const page = () => {
    return (
        <div>
            <Register/>
        </div>
    );
};

export default page;