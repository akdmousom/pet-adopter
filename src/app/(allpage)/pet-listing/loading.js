export default function Loading() {
    // You can add any UI inside Loading, including a Skeleton.
    return (
        <div className="text-3xl flex justify-center items-center min-h-full font-extrabold">

            <h1>Loading...</h1>

        </div>
    );
}